import React from "react";
import HelpSupport from "../components/info/HelpSupport";
import AppLayout from "../components/layout/AppLayout";

const HelpSupportPage = () => {
  return (
    <AppLayout>
      <HelpSupport />
    </AppLayout>
  );
};

export default HelpSupportPage;
